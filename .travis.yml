language: node_js

node_js:
  - 12.16.1

cache: npm

services:
  - xvfb

dist: trusty

addons:
  chrome: stable

install: 
  - npm install

script:
  - npm run test

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  on:
    branch: master